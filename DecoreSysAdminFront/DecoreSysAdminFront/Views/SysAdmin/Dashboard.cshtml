@model IEnumerable<DecoreSysAdminFront.UserServiceRef.UserInfo>

@{
    ViewBag.Title = "Dashboard";
    Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
}

<div class="container">
    <div class="page-header">
       <a href="@Url.Action("AddStudent", "SysAdmin")" class="btn btn-lg btn-success"><span class="glyphicon glyphicon-plus"></span> Lägg till student</a>
       <a href="@Url.Action("AddCoreUser", "SysAdmin")" class="btn btn-lg btn-success pull-right"><span class="glyphicon glyphicon-plus"></span> Lägg till personal</a>
    </div>
</div>

<div class="container">
    <div class="panel panel-default">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th style="color: black;">Användar ID</th>
                    <th>Personnummer</th>
                    <th>Namn</th>
                    <th>Epost</th>
                    <th>Verifierad</th>
                    <th>Student ID</th>
                    <th>Personal ID</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                        <td>@Html.DisplayFor(modelItem => item.SocSecNum)</td>
                        <td>@Html.DisplayFor(modelItem => item.FirstName) @Html.DisplayFor(modelItem => item.LastName)</td>                        
                        <td>@Html.DisplayFor(modelItem => item.Email)</td>
                        <td>@Html.DisplayFor(modelItem => item.EmailVerified)</td>
                        <td>@Html.DisplayFor(modelItem => item.StudentId)</td>
                        <td>@Html.DisplayFor(modelItem => item.EmployeeId)</td>
                        <td>
                            <a href="@Url.Action("Details", "SysAdmin", new { id = item.Id })" class="btn btn-info btn-xs"><span class="glyphicon glyphicon-info-sign"></span> Detaljer</a>
                            <a href="@Url.Action("Edit", "SysAdmin", new { id = item.Id })" class="btn btn-primary btn-xs"><span class="glyphicon glyphicon-edit"></span> Redigera</a>
                            <a href="@Url.Action("Delete", "SysAdmin", new { id = item.Id})" class="btn btn-danger btn-xs"><span class="glyphicon glyphicon-remove-circle"></span> Radera</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

